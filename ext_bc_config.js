// Distributed under MS-RSL license: see /LICENSE for terms. Copyright 2019,20 Dominic Morris.
/*var config = require('./config');
const blockcypher_token = config.get("scp_blockcypher_apikey");
module.exports = {
    base: 'https://api.blockcypher.com/v1/btc/main/',
    address_full: (asset, address) => {
        return `https://api.blockcypher.com/v1/${(asset != "btc_test" ? (asset + "/main") : "btc/test3")}/addrs/${address}/full?token=${blockcypher_token}`
    },
    address_balance: (asset, address) => {
        return `https://api.blockcypher.com/v1/${(asset != "btc_test" ? (asset + "/main") : "btc/test3")}/addrs/${address}/balance?token=${blockcypher_token}`
    },
    new_tx:(asset) => {
        return `https://api.blockcypher.com/v1/${(asset != "btc_test" ? (asset + "/main") : "btc/test3")}/txs/new?token=${blockcypher_token}`
    },
    push_tx:(asset) => {
        return `https://api.blockcypher.com/v1/${(asset != "btc_test" ? (asset + "/main") : "btc/test3")}/txs/push?token=${blockcypher_token}`
    }
}*/